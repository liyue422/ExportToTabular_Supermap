1、图形和界面分离（“确认”键还没有分开）
2、输入数据源（cmbDatasource）选择以后，左边的ListBox自动显示选中数据源中的数据集（只显示几何数据，“_prop”等属性表数据不显示）
3、若输出数据源中已有需转换图层的“_prop”属性表数据，则先删除原“_prop”，再重新生成
    （即修改几何数据时，可以不删除原有的“_prop”,重新生成时会自动覆盖原有的）
注：若有多个数据源，需要跨数据源操作时，需保证不同数据源间没有同名的几何数据集